#pragma once

class Menu {
public:
    Menu();
    ~Menu();

    void show();  // Show the menu.
    void hide();  // Hide the menu.

private:
    // Elements and properties of the menu.
};
