#pragma once

class BehaviorTree {
public:
    BehaviorTree();
    ~BehaviorTree();

    void execute();  // Execute the behavior tree.

private:
    // Nodes and structures for the behavior tree.
};
